# Core bot
tweepy>=4.0.0
pytz>=2023.3
flask>=3.0.0
python-dotenv>=1.0.0

# Testing
pytest>=7.0.0
freezegun>=1.2.0

# President reply uses X API v2 via Tweepy (Bearer Token for reads)
# No additional deps — tweepy already handles both read and write
